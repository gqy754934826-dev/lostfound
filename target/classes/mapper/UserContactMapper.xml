<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.lostfound.mapper.UserContactMapper">

    <resultMap id="BaseResultMap" type="com.example.lostfound.pojo.UserContact">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="contact_user_id" property="contactUserId"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, user_id, contact_user_id, create_time
    </sql>

    <insert id="insert" parameterType="com.example.lostfound.pojo.UserContact" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user_contact (
            user_id, contact_user_id, create_time
        ) VALUES (
            #{userId}, #{contactUserId}, #{createTime}
        )
        ON DUPLICATE KEY UPDATE create_time = #{createTime}
    </insert>
    
    <delete id="delete" parameterType="map">
        DELETE FROM user_contact
        WHERE user_id = #{userId} AND contact_user_id = #{contactUserId}
    </delete>
    
    <select id="selectContactUserIds" resultType="java.lang.Long">
        SELECT contact_user_id
        FROM user_contact
        WHERE user_id = #{userId}
        ORDER BY create_time DESC
    </select>
    
    <select id="exists" resultType="int">
        SELECT COUNT(*)
        FROM user_contact
        WHERE user_id = #{userId} AND contact_user_id = #{contactUserId}
    </select>
</mapper>